services:
  redis:
    image: redis:alpine
    container_name: bbangzip-prod-redis
    restart: always
    networks:
      - bbangzip-prod-network

  app:
    image: ${DOCKER_IMAGE_NAME_PROD}
    container_name: bbangzip-prod-app
    restart: always
    environment:
      SPRING_PROFILES_ACTIVE: prod
    env_file:
      - .env
    depends_on:
      - redis
    networks:
      - bbangzip-prod-network

  nginx:
    image: nginx:1.27-alpine
    container_name: bbangzip-prod-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/prod/conf.d:/etc/nginx/conf.d:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./certbot/conf:/etc/letsencrypt:ro
    depends_on:
      - app
    networks:
      - bbangzip-prod-network

  certbot:
    image: certbot/certbot:latest
    container_name: bbangzip-prod-certbot
    restart: always
    volumes:
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    entrypoint: /bin/sh
    command:
      - -c
      - |
        trap exit TERM
        while :; do
          certbot renew --webroot -w /var/www/certbot --quiet || true
          sleep 12h & wait $$!
        done
    networks:
      - bbangzip-prod-network

networks:
  bbangzip-prod-network:
    external: true
